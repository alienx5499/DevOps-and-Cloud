apiVersion: v1
kind: Service
metadata:
  name: web-service-nodeport
spec:
  type: NodePort
  selector:
    app: web-clusterip
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
